# Technical Debt & Known Gaps

_Last reviewed: 2026-07-21. Investigation-only audit — nothing in this list has been fixed. Findings are drawn from the actual code, SQL, ESLint output, and dependency manifest as of this date._

Priority key: **P0** = blocker (breaks a core workflow or deploy) · **P1** = important (fragile, missing observability, or deprecated) · **P2** = nice-to-have (cleanup or deferred architecture).

## Prioritized Table

| # | Priority | Item | Impact | Suggested fix | Rough effort |
|---|----------|------|--------|---------------|--------------|
| 1 | **P0** | **Missing admin pages: `results` and `standings-review`** | `adminLinks` in `src/app/admin/leagues/[leagueId]/page.tsx` (lines 33–34) links to `/admin/leagues/[leagueId]/results` and `/admin/leagues/[leagueId]/standings-review`, but neither `page.tsx` exists (dir only contains `audit/`, `draft-control/`, `draft-setup/`, `import/`). Both links 404. This breaks the entire post-draft workflow: the commissioner cannot upload results/score or review/publish standings — the two links shown for `drafted`/`scoring`/`completed` league states. The backing API route `src/app/api/admin/leagues/[leagueId]/results/route.ts` already exists, so only the UI is missing for results. | Build `results/page.tsx` (results-upload + score-run UI over the existing results route) and `standings-review/page.tsx` (review + publish over `/api/leagues/[leagueId]/standings`). | M (1–2 days for both) |
| 2 | **P1** | **7 ESLint errors — lint-failing but not currently blocking production builds** | These fail `npm run lint`, but do **not** currently block the production build: `next build` was verified to succeed with them present (this Next 16 / Turbopack setup does not run ESLint as part of `next build`), so they will **not** block a Vercel deploy. Worth fixing for code health and to keep `npm run lint` green. Errors: 4× `react/no-unescaped-entities` (`app/leagues/[leagueId]/draft/page.tsx:172`, `app/leagues/[leagueId]/page.tsx:70` & `:71`, `app/leagues/page.tsx:49`), 2× `react-hooks/set-state-in-effect` (`app/admin/leagues/[leagueId]/draft-control/page.tsx:39`, `app/leagues/[leagueId]/draft/page.tsx:53` — `load()`/`loadData()` calling setState synchronously in an effect), 1× `prefer-const` (`app/api/admin/leagues/[leagueId]/draft/route.ts:96` — `orderedIds`). | Escape apostrophes (`&apos;`), change `let orderedIds` to `const` (auto-fixable via `--fix`), and refactor the two effect bodies to avoid synchronous setState (or move initial load out of the effect). | S–M (~half day) |
| 3 | **P1** | **Draft timer 30-second warning email never fires** | `sendTimerWarningEmail` is defined in `src/lib/email.ts:72` but is **not called anywhere** (only `sendOnClockEmail` is wired into the two draft routes). The draft timer state exists (`draft_state.timer_seconds_remaining` in `001_initial_schema.sql:219`; `draft_timer_seconds`/`draft_timer_on_expiry` in seed config) but there is no server-side clock to detect the 30s threshold. There is **no `vercel.json`** and no cron route, so nothing runs in the background. As built, the warning email is dead code. | Add a Vercel Cron (or Supabase scheduled function) hitting a new route that checks active picks against `timer_seconds_remaining` and calls `sendTimerWarningEmail` once per pick at the 30s mark. Requires a `vercel.json` crons block + idempotency guard. | M (~1 day) |
| 4 | **P1** | **InviteForm server action fetches an internal API route** | In `src/app/admin/leagues/[leagueId]/page.tsx:97–127`, the inline server action does `fetch('/api/auth/invite', …)` with a **relative URL** from server-side code. Relative fetch has no origin on the server and is fragile/environment-dependent; the response is also ignored (`res` unused — see ESLint), so failures are silent with no user feedback and no `revalidatePath`. | Call the invite logic directly (import the handler/service function) instead of round-tripping through HTTP, or use an absolute `NEXT_PUBLIC_APP_URL` base. Surface errors and revalidate the member list. | S (~half day) |
| 5 | **P1** | **Sentry installed but never initialized** | `@sentry/nextjs ^10.53.1` is in `package.json`, but there is **no `instrumentation.ts`, no `sentry.client/server/edge.config.*`, and zero `Sentry` usage in `src/`**. Result: no error monitoring at all despite the dependency cost. Also a stray, unrelated `sentry ^0.33.0` package is listed (likely an accidental/typo install). | Run the Sentry Next.js wizard (creates `instrumentation.ts` + config files + DSN env), or remove the dependency if monitoring isn't wanted. Remove the stray `sentry@0.33.0`. | S (~half day) |
| 6 | **P1** | **`middleware.ts` deprecated in Next 16 (→ `proxy`)** | `src/middleware.ts` uses the `middleware` file convention, which Next 16 (`v16.0.0`) deprecated and renamed to `proxy` (per `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md`). Emits a deprecation warning now; may break in a future major. | Run `npx @next/codemod@canary middleware-to-proxy .` — renames the file and the exported `middleware` function to `proxy`. Verify the matcher config still applies to Supabase session refresh. | S (~1 hour) |
| 7 | **P2** | **TDD-deferred architecture not built** | The TDD describes heavier scoring infrastructure — immutable/versioned **baseline snapshots**, a **`ScoreCalculationRun`** record, and a **`TeamGameResult` game-classification enum** — that was intentionally **not** built (per the TDD's own "smallest compatible plan" guidance; see `CFP_War_Chest_2026_Continuation_Brief.md:59`). Current model maps onto `scoring_configs` JSON + CSV import + `draftable_entities.preseason_win_total`. Gap: no full recalculation history / audit of score runs, and baselines are mutable columns rather than versioned snapshots. | Only revisit if the group wants versioned recalculation history and reproducible score runs. Would add tables + a run-orchestration layer. | L (multi-day) |
| 8 | **P2** | **23 ESLint warnings (all `@typescript-eslint/no-unused-vars`)** | Non-blocking but noisy: unused imports/vars across `src/types/index.ts`, `src/lib/import/parser.ts` (`existingNormalizedNames`), `src/lib/draft/snake.ts`, several `app/leagues/*` pages (`categoryLabel`, `categoryColor`, `picks`, `createClient`), API routes (`z`, `writeAuditLog`, `draftState`, `settings`), and `e2e/draft-flow.spec.ts`. Some (e.g. unused `writeAuditLog` imports in draft/results routes) may hint at intended-but-unwired audit logging. | Remove or wire up the unused symbols; audit the unused `writeAuditLog` imports to confirm audit logging isn't silently missing. | S (~1–2 hours) |

## Notes

- **No `TODO`/`FIXME` markers** were found anywhere in `src/` — the debt above is structural, not annotated in-code.
- **ESLint totals:** 30 problems = 7 errors + 23 warnings (all warnings are `no-unused-vars`).
- Item 1 is the immediate blocker: it leaves the season-scoring half of the app unreachable from the UI. Item 2 (the 7 ESLint errors) fails `npm run lint` but does **not** block the production build — `next build` succeeds with them present — so it is not deploy-blocking; fix it for code health.
